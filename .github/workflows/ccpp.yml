name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: test
      run: ./test
    - name: make
      run: make test.cpp
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
